import type { Metadata } from "next";
import Link from "next/link";
import { notFound, redirect } from "next/navigation";
import { getSupabaseServerClient } from "@/lib/supabase/server";
export const metadata: Metadata = { title: "Listing preview", robots: { index: false, follow: false } };
export default async function AccountSitePage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const supabase = await getSupabaseServerClient(); if (!supabase) redirect(`/login?next=/account/sites/${id}`); const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect(`/login?next=/account/sites/${id}`); const { data: listing } = await supabase.from("website_listings").select("id,owner_id,name,url,normalized_domain,short_description,full_description,contact_email,status,submitted_at,approved_at,category_id").eq("id", id).maybeSingle(); if (!listing || listing.owner_id !== user.id) notFound(); const { data: category } = listing.category_id ? await supabase.from("categories").select("name").eq("id", listing.category_id).maybeSingle() : { data: null }; return <main className="account-shell account-shell-narrow" id="main-content"><header className="account-heading"><span className="eyebrow">Private owner preview · {listing.status.replaceAll("_", " ")}</span><h1>{listing.name}</h1><p>{listing.normalized_domain}</p></header><article className="form-card listing-preview"><p className="listing-preview-category">{category?.name ?? "Requested category"}</p><p className="listing-preview-lead">{listing.short_description}</p>{listing.full_description && <p>{listing.full_description}</p>}<dl><div><dt>Administrative contact</dt><dd>{listing.contact_email}</dd></div><div><dt>Submitted</dt><dd>{new Intl.DateTimeFormat("en-GB", { dateStyle: "medium" }).format(new Date(listing.submitted_at))}</dd></div>{listing.approved_at && <div><dt>Approved</dt><dd>{new Intl.DateTimeFormat("en-GB", { dateStyle: "medium" }).format(new Date(listing.approved_at))}</dd></div>}</dl><div className="form-actions"><Link href="/account" className="button button-secondary">Back to account</Link><Link href={`/account/sites/${listing.id}/edit`} className="button button-accent">Edit listing</Link><a href={listing.url} target="_blank" rel="noreferrer" className="button button-secondary">Visit website</a></div></article></main>; }
